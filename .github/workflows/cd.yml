name: CD Pipeline

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Data Repo
        uses: actions/checkout@v4
        with:
          repository: mohamedamine050/aws_data_eng_project
          token: ${{ secrets.GH_TOKEN }}

      - name: Checkout Terraform Repo
        uses: actions/checkout@v4
        with:
          repository: mohamedamine050/terraform-weather-pipeline-c664c982
          token: ${{ secrets.GH_TOKEN }}
          path: terraform

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        working-directory: terraform/infra
        run: |
          terraform init -input=false

      - name: Debug Terraform outputs
        working-directory: terraform/infra
        run: |
          terraform output

      # ─── Lambda: stream_processor → lambda_process_name ───
      - name: Resolve Lambda stream_processor outputs
        working-directory: terraform/infra
        run: |
          set -euo pipefail
          echo "LAMBDA_FUNCTION_NAME_1=$(terraform output -raw lambda_process_name)" >> $GITHUB_ENV
          echo "LAMBDA_ZIP_URI_1=$(terraform output -raw glue_test_script_s3_uri_process)" >> $GITHUB_ENV
          echo "✓ stream_processor → $(terraform output -raw lambda_process_name) @ $(terraform output -raw glue_test_script_s3_uri_process)"

      - name: Package & upload Lambda stream_processor
        run: |
          set -euo pipefail
          mkdir -p /tmp/lambda_build
          rm -rf /tmp/lambda_build/*
          cp -r src/lambdas/stream_processor/. /tmp/lambda_build/ 2>/dev/null || true
          mkdir -p /tmp/dist
          (cd /tmp/lambda_build && zip -r /tmp/dist/stream_processor.zip . -x '__pycache__/*')
          aws s3 cp /tmp/dist/stream_processor.zip "${LAMBDA_ZIP_URI_1}"
          echo "✓ stream_processor uploaded to ${LAMBDA_ZIP_URI_1}"

      - name: Update Lambda stream_processor code
        run: |
          set -euo pipefail

          BUCKET=$(echo "${LAMBDA_ZIP_URI_1}" | sed 's|s3://||' | cut -d/ -f1)
          KEY=$(echo "${LAMBDA_ZIP_URI_1}" | sed 's|s3://[^/]*/||')

          aws lambda update-function-code \
            --function-name "${LAMBDA_FUNCTION_NAME_1}" \
            --s3-bucket "$BUCKET" \
            --s3-key "$KEY"

          aws lambda wait function-updated \
            --function-name "${LAMBDA_FUNCTION_NAME_1}"

          aws lambda update-function-configuration \
            --function-name "${LAMBDA_FUNCTION_NAME_1}" \
            --handler handler.lambda_handler

          aws lambda wait function-updated \
            --function-name "${LAMBDA_FUNCTION_NAME_1}"

          echo "✓ ${LAMBDA_FUNCTION_NAME_1} updated"

      # ─── Lambda: weather_producer → lambda_fetch_name ───
      - name: Resolve Lambda weather_producer outputs
        working-directory: terraform/infra
        run: |
          set -euo pipefail
          echo "LAMBDA_FUNCTION_NAME_2=$(terraform output -raw lambda_fetch_name)" >> $GITHUB_ENV
          echo "LAMBDA_ZIP_URI_2=$(terraform output -raw glue_test_script_s3_uri_fetch)" >> $GITHUB_ENV
          echo "✓ weather_producer → $(terraform output -raw lambda_fetch_name) @ $(terraform output -raw glue_test_script_s3_uri_fetch)"

      - name: Package & upload Lambda weather_producer
        run: |
          set -euo pipefail
          mkdir -p /tmp/lambda_build
          rm -rf /tmp/lambda_build/*
          cp -r src/lambdas/weather_producer/. /tmp/lambda_build/ 2>/dev/null || true
          mkdir -p /tmp/dist
          (cd /tmp/lambda_build && zip -r /tmp/dist/weather_producer.zip . -x '__pycache__/*')
          aws s3 cp /tmp/dist/weather_producer.zip "${LAMBDA_ZIP_URI_2}"
          echo "✓ weather_producer uploaded to ${LAMBDA_ZIP_URI_2}"

      - name: Update Lambda weather_producer code
        run: |
          set -euo pipefail

          BUCKET=$(echo "${LAMBDA_ZIP_URI_2}" | sed 's|s3://||' | cut -d/ -f1)
          KEY=$(echo "${LAMBDA_ZIP_URI_2}" | sed 's|s3://[^/]*/||')

          aws lambda update-function-code \
            --function-name "${LAMBDA_FUNCTION_NAME_2}" \
            --s3-bucket "$BUCKET" \
            --s3-key "$KEY"

          aws lambda wait function-updated \
            --function-name "${LAMBDA_FUNCTION_NAME_2}"

          aws lambda update-function-configuration \
            --function-name "${LAMBDA_FUNCTION_NAME_2}" \
            --handler handler.lambda_handler

          aws lambda wait function-updated \
            --function-name "${LAMBDA_FUNCTION_NAME_2}"

          echo "✓ ${LAMBDA_FUNCTION_NAME_2} updated"

      # ─── Trigger weather_producer ─────────────────────────────
      - name: Trigger weather_producer
        run: |
          aws lambda invoke \
            --function-name "${LAMBDA_FUNCTION_NAME_2}" \
            --payload '{}' \
            --cli-binary-format raw-in-base64-out \
            /tmp/weather_response.json

          cat /tmp/weather_response.json
          echo "✓ weather_producer triggered"

      # ─── Wait for SQS -> stream_processor ─────────────────────
      - name: Wait for stream processing
        run: |
          echo "Waiting for SQS to trigger stream_processor..."
          sleep 20

      # ─── Verify stream_processor executed ─────────────────────
      - name: Check stream_processor logs
        run: |
          aws logs tail "/aws/lambda/${LAMBDA_FUNCTION_NAME_1}" --since 5m
